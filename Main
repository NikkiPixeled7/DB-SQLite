import sqlite3

conn = sqlite3.connect('my_database.db')

cursor = conn.cursor()

cursor.execute('''
    CREATE TABLE menu (id INTEGER PRIMARY KEY, name TEXT, method TEXT, rating INTEGER)
    INSERT INTO menu (name, method, rating) VALUES ('Executive Blend', 'Percolator', 65)
''')

conn.commit()
conn.close()
